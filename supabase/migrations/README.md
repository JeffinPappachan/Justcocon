# Supabase migrations

This directory is reserved for future SQL migration files.

Important:

- No migrations have been applied to the existing production Supabase project.
- The current Supabase project is marked as PRODUCTION.
- Production database changes are prohibited during Phase 1.
- A separate staging Supabase project is recommended for future development.
- All SQL migration files must be reviewed before execution.
- Supabase MCP integration will be added later for read-only audit support.

Do not run destructive or production-scoped SQL commands from this phase.

{
  "name": "@justcocon/persistence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @justcocon/config build && pnpm --filter @justcocon/shared-types build && pnpm --filter @justcocon/validation build && pnpm --filter @justcocon/booking-domain build && pnpm build && node --test dist/**/*.test.js",
    "typecheck": "pnpm --filter @justcocon/booking-domain build && tsc --noEmit"
  },
  "dependencies": {
    "@justcocon/booking-domain": "workspace:*",
    "@justcocon/config": "workspace:*",
    "@justcocon/shared-types": "workspace:*",
    "@justcocon/validation": "workspace:*",
    "@supabase/supabase-js": "^2.49.8"
  },
  "devDependencies": {
    "@types/node": "^22.8.1",
    "typescript": "^5.6.2"
  }
}
